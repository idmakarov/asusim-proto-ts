const dataGridStyle: { [key: string]: string | number; } = {
    minWidth: "100%",
    minHeight: "auto",
    borderRadius: 10,
    borderWidth: 2,
    color: "#000000"
};

export default dataGridStyle;